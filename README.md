# Syntinel Server

## Setup

### Development Setup

Make sure you have Make and OpenSSL installed and run:

```
./setup.sh
```

### Run Server

Make sure you have Golang installed and run:

```
make run
```

